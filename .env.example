# MediTalk Environment Configuration Example

# Hugging Face authentication token
# Get your token from: https://huggingface.co/settings/tokens
# You need to request access to the gated model: https://huggingface.co/canopylabs/orpheus-3b-0.1-ft
HUGGINGFACE_TOKEN=your_huggingface_token_here

# Orpheus TTS Configuration
# Languages to load at startup: comma-separated list of "en", "fr"
# Default: only English. Set to "en,fr" to load both models
ORPHEUS_LANGUAGES=en,fr

# MultiMeditron
# Default model: ClosedMeditron/Mulimeditron-End2End-CLIP-medical (private HF repo)
MULTIMEDITRON_MODEL=ClosedMeditron/Mulimeditron-End2End-CLIP-medical

# MultiMeditron HuggingFace token (for accessing private model)
# Ask LiGHT Laboratory to give the access token to: https://huggingface.co/ClosedMeditron/Mulimeditron-End2End-CLIP-medical
MULTIMEDITRON_HF_TOKEN=your_multimeditron_token_here
# Note: Also ensure your account has access to https://huggingface.co/meta-llama/Meta-Llama-3.1-8B-Instruct (base model for MultiMeditron)

# Whisper ASR model size: tiny, base, small, medium, large
# tiny: ~39 MB, base: ~74 MB, small: ~244 MB, medium: ~769 MB, large: ~1550 MB
# Note: Whisper model is responsible for both language identification and speech-to-text
WHISPER_MODEL=base

# Service URLs (recommendation: do not change)
ORPHEUS_URL=http://localhost:5005
WHISPER_URL=http://localhost:5007
BARK_URL=http://localhost:5008
MULTIMEDITRON_URL=http://localhost:5009
CSM_URL=http://localhost:5010
QWEN3OMNI_URL=http://localhost:5014

# Audio storage directories (recommendation: do not change)
AUDIO_DIR_ORPHEUS=../../outputs/orpheus/
AUDIO_DIR_BARK=../../outputs/bark/
AUDIO_DIR_CSM=../../outputs/csm/
AUDIO_DIR_QWEN3OMNI=../../outputs/qwen3omni/

# Controller Port (recommendation: do not change)
CONTROLLER_PORT=8000

# Controller verbose logging (true/false)
CONTROLLER_VERBOSE_LOGGING=false
